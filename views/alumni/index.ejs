<% layout("/layouts/boilerplate") %>

<section class="alumni-section py-5">
  <div class="container">
    <div class="text-center mb-5" data-aos="fade-up">
      <h1 class="fw-bold text-primary mb-3">Our Alumni Network</h1>
      <p class="text-muted">Explore professionals who once walked your path — connect, collaborate, and grow together.</p>
    </div>

    <!-- Search and Filter -->
    <!-- <div class="search-filter mb-4" data-aos="fade-up">
      <form action="/alumni" method="GET" class="d-flex flex-wrap justify-content-center gap-3">
        <input type="text" name="q" class="form-control search-input" placeholder="Search alumni by name, company..." style="max-width: 350px;">
        <select class="form-select" name="industry" style="max-width: 200px;">
          <option value="">All Industries</option>
          <option>Technology</option>
          <option>Finance</option>
          <option>Education</option>
          <option>Healthcare</option>
        </select>
        <button type="submit" class="btn btn-gradient px-4">Search</button>
      </form>
    </div> -->

    <!-- Alumni Grid -->
    <div class="row g-4 justify-content-center">
      <% for (let alumni of alumnis) { %>
        <div class="col-xl-3 col-lg-4 col-md-6 col-sm-10" data-aos="zoom-in" data-aos-delay="100">
          <a href="/alumni/<%= alumni._id %>" class="text-decoration-none">
            <div class="alumni-card shadow-sm">
              <div class="alumni-image" style="display:flex; justify-content: center;">
                <img src="<%= alumni.profilePic.url %>" alt="<%= alumni.name %>" class="img-fluid rounded-circle">
              </div>
              <div class="alumni-info text-center mt-3">
                <h5 class="fw-semibold text-dark"><%= alumni.name %></h5>
                <p class="text-muted mb-1"><i class="fa-solid fa-building"></i> <%= alumni.company || 'Independent' %></p>
                <p class="text-primary small"><i class="fa-solid fa-user-tie"></i> <%= alumni.role || 'Alumni' %></p>
              </div><br><br>
            </div>
          </a>
        </div>
      <% } %>
    </div>
  </div>
</section>
